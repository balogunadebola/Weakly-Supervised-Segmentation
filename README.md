# Weakly-Supervised-Segmentation
This project explores point-based weak supervision for semantic segmentation on the 
DeepGlobe Land Cover dataset. Unlike dense pixel-wise masks, point supervision significantly 
reduces the cost of manual labelling. This report evaluates a custom Partial Focal Cross-Entropy 
Loss and analyses how the quantity of point labels per class affects segmentation accuracy.
